describe('Test GET /TEST1', () => {
  test('Should response with 200 success', () => {
    const response = 200
    expect(response).toBe(200)
  })
})

describe('Test POST /TEST2', () => {
  test('Should response with 200 success', () => {
    const response = 200
    expect(response).toBe(200)
  })
})
